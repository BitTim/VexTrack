<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.VexTrack" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/aqua1</item>
        <item name="colorOnPrimary">@color/white</item>

        <item name="colorSecondary">@color/aqua2</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/darkShade</item>

        <!-- Customize your theme here. -->
        <item name="colorSurface">@color/darkShade</item>
        <item name="colorOnBackground">@color/darkFG</item>

        <item name="android:windowBackground">@color/darkBG</item>
        <item name="android:textColor">?attr/colorOnBackground</item>

        <item name="colorDraw1">@color/darkDraw1</item>
        <item name="colorDraw2">@color/darkDraw2</item>
    </style>
</resources>