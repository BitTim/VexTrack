import 'package:flutter/material.dart';

class HistoryEntryWidget extends StatelessWidget
{
  const HistoryEntryWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context)
  {
    return const Scaffold(
      body: Center(
        child: Text("History"),
      ),
    );
  }
}